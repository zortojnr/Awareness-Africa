I will implement the requested changes to the website, ensuring consistency and functionality as specified.

**Plan:**

1.  **Unified Hero Backgrounds:**
    *   **Goal:** Apply the `Programs.tsx` hero background style (AAF Blue Logo with `opacity-[0.12]`) to `About.tsx` and `Resources.tsx`.
    *   **`About.tsx`:** I previously updated this, but I will double-check that it exactly matches the `Programs.tsx` style (specifically using the `blueLogo` import if the dynamic content hook isn't strictly required for this visual consistency, or ensuring the default matches). *Note:* I'll ensure the `opacity` and positioning classes are identical to `Programs.tsx`.
    *   **`Resources.tsx`:** I will add the `blueLogo` import and the background `div` code block to the hero section, matching the implementation in `Programs.tsx`.

2.  **Donate Button Logic ("Sponsor a Child"):**
    *   **Goal:** Reveal a "Sponsor a Child" button when "Donate" is clicked.
    *   **Component:** `InvolvementModal.tsx`.
    *   **Implementation:**
        *   Currently, the modal has a "Direct Financial Support" section.
        *   I will find the main "Donate" interaction point (likely inside the modal or the trigger). The user says "when the donate button is clicked".
        *   *Interpretation:* The "Donate" button usually opens the `InvolvementModal`. Inside that modal, there are donation options.
        *   *Refinement:* If the user means the global "Donate" button in the Navbar/Hero, it opens the modal. I should check if they want the *modal* to show this new button, or if clicking a button *inside* the modal reveals it.
        *   *Assumption:* Based on "reveal an additional button", I will modify the `InvolvementModal`'s "Direct Financial Support" section. I'll add a state `showSponsor` (initially false). When the user clicks the existing "Secure Donation Link" (or a new "Donate" trigger inside), it will reveal the "Sponsor a child" button.
        *   *Correction:* The user says "when the donate button is clicked". This might refer to the main CTA. However, since "Donate" usually leads to bank details, I will look at the `InvolvementModal` content. I will modify the "Secure Donation Link" button or add a primary "Donate Now" button that, when clicked, expands to show "Sponsor a Child".
        *   *Wait:* The "Donate" button in the Navbar opens the modal. I will assume the user wants this interaction *inside* the modal or wherever the donation flow starts. I'll stick to the `InvolvementModal`. I will add a "Donate" button that, upon click, reveals the "Sponsor a child" button alongside or below it.

3.  **Account Details Update:**
    *   **Goal:** Update bank info in `InvolvementModal.tsx`.
    *   **Data:**
        *   Account: 2182926086
        *   Bank: UBA
        *   Name: Victoria Odesanya (Founder, Awareness Africa)
        *   Reference: For: Awareness Africa Foundation
    *   **Action:** Replace the hardcoded text in the "Local Bank Account" card in `InvolvementModal.tsx`.

**Execution Steps:**
1.  Update `Resources.tsx` (Hero Background).
2.  Update `About.tsx` (Verify Hero Background consistency).
3.  Update `InvolvementModal.tsx` (Account details & Button logic).

*Self-Correction on Button Logic:*
"when the donate button is clicked, reveal an additional button labeled 'Sponsor a child'".
If there is a "Donate" button in the modal (currently "Secure Donation Link"), I will make clicking it toggle the visibility of a secondary "Sponsor a child" button instead of navigating immediately, or just add the logic there.
*Actually*, looking at the code for `InvolvementModal`, there is a "Secure Donation Link" button. I will wrap this or add a handler so that clicking it reveals the new option.

Let's proceed.